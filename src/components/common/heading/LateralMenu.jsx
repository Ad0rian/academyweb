/*import React from "react";
import "./header.css";
import * as constants from"./Header.jsx";

const LateralMenu = (Lat) => {
    Lat = document.getElementById("Lateral");

    return (

      <div className="sizeboxLateral">
      <ul className="flexSB center latMenu">
              <li className="buttonHeader" onClick={() => constants.scrollTopositionLateral('top')} >Home </li>
              <li className="buttonHeader" onClick={() => constants.scrollTopositionLateral('courses')} >Classes</li>
              <li className="buttonHeader" onClick={() => constants.scrollTopositionLateral('aboutSection')} >About</li>
            </ul>

      </div>
    );
}

export default LateralMenu*/